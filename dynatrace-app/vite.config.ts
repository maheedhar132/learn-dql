import { defineConfig } from '@dynatrace/app-toolkit';

export default defineConfig({
  app: {
    name: 'DQL Investigator',
    version: '1.0.0',
    id: 'com.dynatrace.dql-investigator',
    description: 'Interactive learning platform for DQL and DPL',
  },
});
